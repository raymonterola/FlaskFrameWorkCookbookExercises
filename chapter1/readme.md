This project demonstrates the ff:

* instance folders - enables different configuration files for different environments
* views and models structure - app.hello demonstrates the organization of views and models